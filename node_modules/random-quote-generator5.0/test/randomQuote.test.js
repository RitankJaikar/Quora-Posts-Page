const getRandomQuote = require('../index'); // Adjust the path based on your project structure

test('getRandomQuote returns a quote', () => {
  const quote = getRandomQuote();
  expect(typeof quote).toBe('string');
});
